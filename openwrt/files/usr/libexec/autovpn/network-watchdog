#!/bin/sh

# The controller lock serializes this worker with subscription and LuCI actions.
# Exit code 3 means that no unconfirmed transaction remains.
while :; do
	sleep 5
	/usr/sbin/autovpnctl network-tick >/dev/null 2>&1
	[ "$?" -eq 3 ] && exit 0
done
